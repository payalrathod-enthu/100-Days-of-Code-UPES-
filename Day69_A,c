#include <stdio.h>

int main() {
    int n;
    scanf("%d", &n);

    int arr[n];
    int freq[n+1]; // since elements are in range 0..n
    int i;

    // initialize frequency array
    for(i = 0; i <= n; i++) {
        freq[i] = 0;
    }

    // read array and detect repeated element in one iteration
    for(i = 0; i < n; i++) {
        scanf("%d", &arr[i]);

        freq[arr[i]]++;

        if(freq[arr[i]] == 2) {  
            printf("%d", arr[i]);
            return 0; // stop program immediately
        }
    }

    return 0;
}
